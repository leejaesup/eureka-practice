spring:
  application:
    name: user-service
server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
  client:
    register-with-eureka: true
    fetch-registry: true #EUREKA ????? ?????? ??? ????? ??? ???? ???? ??. true? ?? ? ??? ??? ???? ??
    service-url:
      defaultZone: http://10.0.1.58:8761/eureka
